import { HeroSlider } from "../components";

const Home = () => {
  return (
    <main className="h-[calc(100vh-5rem)] bg-amber-50 mt-[5rem]">
      <HeroSlider />
    </main>
  );
};

export default Home;
